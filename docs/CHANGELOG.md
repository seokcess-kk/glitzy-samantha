# 변경 이력 (CLAUDE.md)

규칙 추가/수정 시 날짜와 사유를 기록. 불필요해진 규칙은 삭제하되 이력에 사유 남길 것.

| 날짜 | 내용 |
|------|------|
| 2026-06-30 | 대시보드 ROAS 산정 기준 차이 ⓘ 고지 — 상위 KPI 카드 ROAS(`app/api/dashboard/kpi`)는 **기간 내 전체 결제 매출 ÷ 광고비**(채널 귀속 무관, 2026-06-15 정정)인 반면, 하단 채널 성과 테이블(`app/api/dashboard/channel`)의 ROAS는 **각 채널 유입 리드에 귀속된 매출 ÷ 채널 광고비**라 두 값이 구조적으로 다를 수 있음(예: 브릴린의원 #21 = 655% vs 389% — 전체 매출 중 광고 리드 귀속분이 약 59%). 사용자 혼동 방지를 위해 기존 `components/common/info-hint.tsx`(`InfoHint`) 패턴을 재사용해 ⓘ 고지 2건 추가: ① `components/dashboard/kpi-section.tsx` ROAS 카드에 `hint="기간 내 전체 매출 기준"`(`StatsCard.hint` slot), ② `components/dashboard/channel-table.tsx` "채널 성과" 제목 옆 `InfoHint text="채널 귀속 매출 기준 (상위 ROAS는 전체 매출)"`. 계산 로직·API·DB 불변, 표시 고지만 추가. 버그가 아닌 의도된 산정 정의 차이를 표면화. |
| 2026-06-25 | 순위 모니터링 "전체 보기" 카테고리 그룹핑 — `app/(dashboard)/monitoring/page.tsx` 월간 순위 테이블이 키워드를 `/api/monitoring/rankings`의 반환 순서(등록=id 순) 그대로 렌더하면서, `category === 'all'`일 때 직전 행과 카테고리가 바뀔 때마다 섹션 헤더를 출력(`showCategoryHeader = kw.category !== prevCategory`). 등록 순서가 섞여 있으면(예: 루비의원 스마트블록→플레이스→스마트블록) 동일 섹션이 분리돼 두 번 노출되던 문제. 프론트엔드에 `displayKeywords` memo 추가 — "전체 보기"에서만 `CATEGORY_LIST`(place→website→smartblock→related) 순으로 그룹핑, V8 stable sort라 카테고리 내부 등록 순서는 유지. 렌더링(`.map`)·편집 셀 Tab/Enter 순서(`editableCellOrder` → `buildEditableCellOrder(displayKeywords…)`) 모두 동일 기준으로 일관 적용. 특정 카테고리 필터 시엔 `keywords` 그대로(헤더 미출력). API·DB 스키마·요약 지표 불변, 표시 순서만 변경. |
| 2026-06-23 | 랜딩페이지 관리 페이지 좌측 병원 드롭다운 연동 — `app/(dashboard)/admin/landing-pages/page.tsx`가 `useClinic()`의 `selectedClinicId`를 미구독해, 드롭다운으로 병원을 바꿔도 전체 랜딩페이지가 그대로 노출되던 문제. ① `GET /api/admin/landing-pages`에 `clinic_id` 쿼리 필터(`parseId` → `.eq('clinic_id', …)`) 추가, ② 페이지가 `useClinic()` 구독 + `fetchData`를 `useCallback([selectedClinicId])`로 전환해 fetch에 `clinic_id` 전달 + 드롭다운 변경 시 자동 재조회, ③ 신규 등록 다이얼로그 오픈 시 선택된 병원을 배정 병원 기본값으로 채움. 광고 소재(`admin/ad-creatives`, 2026-05-18) 패턴과 동일. 전수 감사 결과 나머지 24개 대시보드 페이지는 이미 연동(19개) 또는 연동 불필요(전역/전체관리: clinics·users·login-logs·settings·staff·utm)로 확인 — 랜딩페이지가 유일한 누락이었음. demo_viewer 분기는 기존에 이미 `clinic_id` 처리. |
| 2026-06-23 | 클리닉별 광고비 마크업(관리 수수료) 읽기 시점 가산 — 합의된 일일 정액을 DB 원본은 그대로 둔 채 **조회 시점에만** 대상 캠페인 spend에 가산해, spend에서 파생되는 ROAS·CPL·CPC·CAC가 자동 일관 반영되도록 함. 신규 테이블 `clinic_ad_markup`(`supabase/migrations/20260623_clinic_ad_markup.sql`: clinic_id·platform·campaign_id·daily_amount·effective_from·effective_to·label·is_active, RLS 활성)과 공용 헬퍼 `lib/ad-markup.ts`(`fetchAdMarkups`/`buildMarkupStatRows`(수수료 전용 합성 행)/`markupTotal`/`markupCampaignIds`/`MARKUP_HINT`) 도입. spend를 읽는 10개 라우트에 일관 적용해 화면 내·탭 간 합계 불일치 방지(매출 귀속 ROAS도 메인 KPI와 동일 비용 기준). ① **캠페인/집계 레벨**(`dashboard/kpi`·`trend`·`channel`, `ads/stats`·`efficiency-trend`·`platform-summary`·`day-analysis`, `attribution/summary`·`roas-trend`)은 일일 정액을 그대로 가산. ② **소재 레벨**(`ads/creatives-performance`)은 일일 정액 총액 M(=정액×적용일수)을 대상 캠페인 소재들의 **비용소진(spend) 비중으로 안분**해 합계가 캠페인 가산액과 정확히 일치(소재 귀속은 `ad_stats.campaign_id`, 비중은 적용 기간 spend 기준). **고지 원칙**: 가산된 광고비가 보이는 곳에는 ⓘ "관리비 포함" 고지를 동반 — 신규 `components/common/info-hint.tsx`(hover+모바일 탭+키보드 focus 모두 노출, 접근성), `StatsCard.hint` prop 추가, 메인/광고 KPI '광고비'·'총 광고비' 카드(`spendIncludesMarkup` 플래그)·캠페인 랭킹 지출 셀(`markup.campaignIds`)·소재별 성과 지출 셀(`markup.creativeKeys`)·매출 귀속 '총 광고비' 카드에 부착. **정산용 `external/ad-spend`(실집행비)·랜딩페이지는 가산 미적용(raw 유지)** — 실집행비는 별도 리포트 기준. 초기 설정: 클리닉 #21, 캠페인 34778572(2605_ultherapy), 2026-06-13부터 일 40,000원(중지 시 `effective_to`/`is_active`로 종료, 코드 수정 불필요). |
| 2026-06-15 | 대시보드 KPI ROAS 분자 정정 — `app/api/dashboard/kpi/route.ts`의 ROAS가 "기간 내 인입된 리드 고객(`leadCustomerIds`)에게서 발생한 매출(`leadRevenue`)" 만 분자로 써서, 같은 기간 광고비로 발생했더라도 리드로 기록되지 않은 고객(전화·재방문·오프라인 유입 등)의 매출이 누락돼 ROAS가 과소 집계되던 문제. 분자를 기간 내 전체 결제 매출(`totalRevenue` = `payments.payment_amount` 합)로 변경해 **광고비 대비 매출액** 정의로 정정(`totalSpend > 0 ? totalRevenue / totalSpend : 0`). 미사용이 된 `leadCustomerIds`/`leadRevenue` 블록 제거. demo fixture(`lib/demo/fixtures/aggregates.ts::demoKpi` = `agg.revenue / agg.spend`)·주간 리포트(`lib/services/weeklyReport.ts` = `totalRevenue / totalSpend`)·채널/캠페인/어트리뷰션 라우트는 이미 전체 매출 기준이라 프로덕션 KPI 한 곳만 불일치했던 것 → 전사 정의 일치. 카드 표시 형식(`roas * 100`%)·비교(`compare`) 로직 불변, DB 스키마 불변. **(동일 작업)** 대시보드 ROAS 카드 클릭 시 404 fix — `components/dashboard/kpi-section.tsx`의 ROAS 카드가 존재하지 않는 `/attribution` 경로로 링크돼 `router.push` 시 404. 매출 귀속 화면은 별도 페이지가 아니라 `/ads`의 "매출 귀속" 탭(`AttributionView`, `?tab=attribution` 복원)으로 구현돼 있어 경로를 `/ads?tab=attribution`로 정정. 나머지 4개 카드(광고비·CPL→`/ads`, 리드→`/leads`, 매출→`/patients`)는 정상. |
| 2026-06-09 | 캠페인/리드 카드에 Meta `fbclid` 노출 차단 — 운영 랜딩페이지 폼(예: `id=34778572` sleep-ulthera LP)이 Meta가 클릭 URL에 자동으로 붙이는 `fbclid`(AEM 형식, `_aem_` 접미사)를 `custom_data`에 그대로 담아 `/api/webhook/lead`로 전송 → DB `leads.custom_data.fbclid`에 저장(최근 리드 300건 중 283건). `lib/lead-custom-data.ts::extractCustomEntries`의 `CUSTOM_DATA_INTERNAL_KEYS`에 `fbclid`가 없어 사용자 선택값처럼 긴 추적 문자열이 칩으로 노출되던 문제. 제외 키에 광고 클릭 ID(`fbclid`/`gclid`/`ttclid`/`wbraid`/`gbraid`/`msclkid`/`yclid`/`dclid`)와 UTM 키(`utm_id`/`utm_source`/`utm_medium`/`utm_campaign`/`utm_content`/`utm_term`) 추가. 더불어 리드 536건 custom_data 전수 조사에서 `product`의 다중 상품 버전인 `bundle`(내부 상품 슬러그 배열 `["thermage-flx-600shot"…]`)이 칩으로 노출되던 것(11건)도 함께 제외. 전수 시뮬레이션 결과 표시 라벨이 가격·상품명·survey(step1~5)만 남음(fbclid/슬러그 노출 0건 확인). `price`·survey 답변 등 실제 표시값은 영향 없음. 표시 로직만 변경, webhook 저장 구조·DB 스키마 불변. `campaigns`/`leads` 두 페이지 공통 적용. |
| 2026-06-05 | 리드 추가 정보(custom_data) 표시 견고화 — 랜딩페이지 폼이 표준 `custom_data.survey` 구조가 아닌 평면 키/불리언/중첩 객체를 보내도 깔끔히 표시되도록 공용 포맷터 `lib/lead-custom-data.ts`(`extractCustomEntries`/`formatCustomValue`) 신설. 기존엔 `app/(dashboard)/leads/page.tsx`가 `custom_data.survey` 객체만, `app/(dashboard)/campaigns/page.tsx`가 `Object.values(survey)`만 렌더해 ① survey 밖 평면 필드 미표시 ② 중첩 객체 `[object Object]` 노출 ③ 카드 헤더에 이미 있는 `name` 중복 위험이 있었음. 포맷터는 survey 등 중첩 객체를 한 단계 평탄화, 불리언→예/아니오, 배열→`, ` join, 내부 키(name·marketing_consent·idempotency_key·event_id) 제외, 빈 값 제거. `leads` 카드는 라벨/값 행 + "마케팅 수신 동의"(별도 처리), `campaigns`는 값 칩으로 동일 유틸 사용. 빈 항목일 때 빈 박스 미노출. webhook 저장 구조·DB 스키마 불변, 표시 로직만 변경. **(동일자 보강)** 이벤트 LP가 `custom_data`에 사용자 선택값(`selected_option_name`·`price`)뿐 아니라 전체 메뉴 카탈로그(`options` = `[{name,price}…]` 배열)와 템플릿 설정값(`product`·`price_note`·`event_period`)까지 통째로 실어보내 카드에 전체 메뉴가 덤프되던 문제 — 포맷터를 "선택값만" 정책으로 조정: `options`·`product`·`price_note`·`event_period` 및 객체 배열(카탈로그)을 표시 제외, `selected_option_name`→"상품명"·`price`→"가격"(`1,000` 단위 + "원") 라벨/통화 포맷, 선택 상품·가격을 앞으로 정렬. 스칼라 배열(다중선택 답변)은 join 유지. |
| 2026-06-04 | CI: E2E 로그인 성공/로그아웃 테스트 분리 — `auth.spec.ts`의 "유효한 자격 증명으로 로그인 성공"·"로그인 후 로그아웃 성공"이 자격 증명(`E2E_SUPERADMIN_*`)을 요구함에도 `chromium-no-auth` 프로젝트에 속해, 워크플로우 authenticated step의 `E2E_SUPERADMIN_EMAIL != ''` 가드를 우회해 secrets 비어있는 CI에서 강제 실행 → 더미 계정(`admin`/`test-password`)으로 로그인 실패 후 `/login` 잔류로 매 push마다 timeout. 두 테스트를 `e2e/tests/auth-login.spec.ts`로 분리(정규식 경계상 `auth-login`은 `chromium-no-auth`의 `(auth\|landing-page)\.spec\.ts` testMatch에 미매칭, `chromium`의 `(auth\|landing-page\|demo)\.spec\.ts` testIgnore에도 미매칭 → 자동으로 `chromium` 프로젝트에 포함되어 기존 계정 가드가 보호). chromium 프로젝트는 storageState(`.auth/superadmin.json`)를 주입하므로, 로그인 플로우 검증용 새 파일은 `test.use({ storageState: { cookies: [], origins: [] } })`로 미인증 상태 시작. `auth.spec.ts`에는 자격 증명 불필요한 실패·리다이렉트 케이스만 잔류. `playwright.config.ts` 두 프로젝트에 분류 의도 주석 추가. 광고 동기화 등 기능 코드와는 무관(인증 secrets 누락이 근본 원인). |
| 2026-06-04 | 리드 webhook clinic_id 서버 복구 — 랜딩페이지 HTML이 `__LP_DATA__` 주입 실패로 하드코딩 fallback 문자열(`clinic_id:'brillyn'` 등)을 보내면 `parseId` 실패로 리드가 400 거부되어 통계에서 누락되던 문제. `app/api/webhook/lead/route.ts` 의 strict `clinic_id` 필수 검증을 제거하고 try 진입부에 복구 로직 추가: ① `landing_page_id` 무효/누락 시 `inflowUrl` 의 `id=` 에서 추출(`parseLandingPageIdFromUrl` 신규, `lib/utm.ts`), ② 확정된 landing_page_id 로 `landing_pages` 조회해 소속 병원을 source of truth 로 `clinic_id` 보정(불일치 시 warn), ③ 존재하지 않는 landing_page_id 는 FK 위반 방지로 null 처리, ④ 복구 불가 시에만 거부(원본은 `lead_raw_logs` 보존). `__tests__/lib/utm.test.ts` 에 `parseLandingPageIdFromUrl` 케이스 9건 추가. 제출 차단 대신 서버가 귀속을 복구하므로 고객 정보 유실 0. |
| 2026-06-04 | 광고 자동 동기화 시각 변경 — `vercel.json` 의 `sync-ads` 크론을 `0 3 * * *` → `0 23 * * *` 로 변경. Vercel Cron은 UTC 기준이라 기존 `0 3` 은 KST 정오(12:00)에 실행돼 전일자 데이터가 오전 업무 시간에 아직 수집되지 않던 문제. KST 08:00(=UTC 23:00)로 당겨 오전 확인 전 반영 완료되도록 정합화. 날짜 계산 로직(`sync-ads/route.ts` `setDate(-1)` + `getKstDateString`)은 시각 변경과 무관하게 정상(KST 08:00 실행 → 전일 D-1 수집). 주석/`CLAUDE.md` Cron Jobs 표의 잘못된 "새벽 3시"/"매일 03:00" 표기도 실제 KST 시각으로 정정(언론보도 `0 0` = KST 09:00 병기). |
| 2026-06-02 | 랜딩페이지 업로드 `FUNCTION_PAYLOAD_TOO_LARGE` fix — 신규 LP HTML 업로드가 Vercel 서버리스 함수 요청 바디 한도(4.5MB 고정, 설정 불가)를 초과해 `Request Entity Too Large`로 차단되던 문제. base64 인라인 자산이 많은 LP HTML은 multipart 인코딩 오버헤드까지 더해 쉽게 초과. `app/api/admin/landing-pages/upload/route.ts`를 **파일 바이트 수신 → 서명 업로드 URL 발급**(`createSignedUploadUrl(fileName, { upsert:true })`, JSON `{originalName, overwrite?}` 입력)으로 전환하고, `app/(dashboard)/admin/landing-pages/page.tsx` handleSave가 브라우저 `supabase` anon 클라이언트의 `uploadToSignedUrl(path, token, file, {contentType:'text/html', upsert:true})`로 Supabase Storage에 **직접 업로드**(Vercel 함수 우회) → 페이로드 한도와 무관. 클라이언트 크기 한도 5MB→20MB, UI 힌트/검증 동기화. `sanitizeFileName` 한글 파일명 분기의 이중 점(`...143522..html`) 버그도 정정(밀리초 제거 후 14자리). 메타데이터 저장 POST 흐름은 불변. |
| 2026-05-28 | 광고 성과 일자별 데이터 정합화 — `/api/ads/stats` 가 `days` 파라미터만 받아 `Date.now() - days * 86400000`로 시작일을 계산했기 때문에 사용자가 DateRangePicker 로 과거 기간(예: 4/1~4/30)을 선택해도 항상 "오늘 - days일 ~ 오늘" 범위만 반환되던 캠페인 분석 탭의 날짜 무시 버그 fix. API 가 `startDate`/`endDate` 를 우선 받고 `stat_date` `gte`/`lte` 동시 적용, 리드 카운트도 `created_at` 을 KST `+09:00` 기준 [startDate, endDate+1) 범위로 필터. `days` 는 fallback 으로 유지(호환성). `components/ads/campaign-ranking-table.tsx`·`ads-funnel.tsx`·`ads-campaign-tab.tsx` 3곳을 `startDate`/`endDate` 전달로 변경, `AdsCampaignTab`의 `days` prop 제거. `/api/ads/efficiency-trend` 의 dayMap 생성 루프를 CLAUDE.md 금지 패턴(`new Date('YYYY-MM-DD')` UTC 자정 + `setDate(getDate()+1)`)에서 KST 자정 명시 + `getKstDateString` 비교 루프로 정비, 성과 개요 효율 추이 차트의 일자 매핑 안전성 강화. `lib/demo/fixtures/aggregates.ts::demoAdStats` 시그니처도 `(clinicId, days, platform)` → `(clinicId, startDate, endDate, platform)` 로 변경. |
| 2026-05-21 | Meta CAPI fire-and-forget abort fix — `app/api/webhook/lead/route.ts`의 SMS 알림 IIFE/`sendCapiEvent`/`sendErrorAlert`(catch) 3곳을 `@vercel/functions`의 `waitUntil(promise)`로 감싸 응답 반환 후에도 promise 완료 보장. Vercel 서버리스 환경에서 응답 직후 함수 인스턴스가 동결/종료되면서 진행 중이던 `fetch`가 강제 abort되어 `capi_events.status='fail'`, `error_message='This operation was aborted'`로 누적되던 문제(6일 3건, statusCode 없음 → HTTP 응답 도달 전 abort) 해소. `@vercel/functions ^3.6.0` 신규 의존성 추가. 광고 운영 영향은 미미(Meta Pixel 이중 백업, event_id dedup)했지만 관리자 SMS(`meta_capi_fail`) 알림 감소 + CAPI 학습 데이터 정합성 향상. |
| 2026-05-21 | E2E 로그인 셀렉터 정합화 — 로그인 폼은 `id="username"`만 갖고 `name` 속성이 없어 e2e `input[name="email"]` 셀렉터가 영원히 매칭 안 돼 GitHub Actions `chromium-no-auth` 6 테스트(로그인 폼 표시/성공/실패/빈 아이디/빈 비밀번호/로그아웃)가 매 push마다 timeout. `app/login/page.tsx` 아이디·비밀번호 Input에 `name="username"`/`name="password"` 추가하고 e2e 4개 파일(`pages/login.page.ts`·`fixtures/auth.fixture.ts`·`tests/auth.setup.ts`·`tests/auth.spec.ts`) 셀렉터·필드명(`emailInput`→`usernameInput`, `TEST_USERS.*.email`→`username`)을 일괄 정렬. `errorMessage` 로케이터에 실제 사용되는 `.text-red-400` 클래스 추가(`text-red-500/10`은 배경이라 미매칭). 테스트명 "빈 이메일로 로그인 시도 실패" → "빈 아이디로 로그인 시도 실패". |
| 2026-05-21 | Agatha 2026-05-21 패치 3건 이식: (1) ERP webhook `client.created` 자동 등록 비활성화(`app/api/webhook/erp-client/route.ts`) — 잘못 꼬여 등록된 거래처 자동 유입 차단, clinic은 admin 에서 명시적 생성 후 ERP 매핑. (2) Google Ads `customer_id` 만 필수로 축소 + 5개 공통 필드(client_id/client_secret/developer_token/refresh_token/login_customer_id) `GOOGLE_ADS_*` ENV fallback (`lib/platform.ts`/`lib/services/googleAds.ts`/`lib/services/adSyncManager.ts`/`app/api/admin/clinics/[id]/api-configs/test/route.ts`) — MCC 공통 자격증명 공유로 클라이언트별 입력 부담 감소, `login_customer_id` 신규 추가. (3) 광고 백필 다이얼로그 + 매체 선택(`components/ads/backfill-dialog.tsx`/`app/api/admin/clinics/[id]/backfill-ads/route.ts`/`app/api/ads/configured-platforms/route.ts`) — `SyncOptions.platforms` 필터(`adSyncManager.syncClinic`)로 특정 매체만 호출해 function timeout 위험 축소, admin/clinics 페이지 테이블 행에 백필 트리거 버튼 노출. |
| 2026-05-19 | 결제 수정/삭제 권한 확장: `PATCH /api/payments/[id]` 신규 추가(시술명·금액·결제일 부분 업데이트, KST 자정 기준 저장), `DELETE` 권한을 `withSuperAdmin` → `withAuth` + `checkClinicAccess`로 변경해 `clinic_admin`/`clinic_staff`도 자기 병원 결제 삭제 가능. `payments.updated_by` 컬럼 추가(`20260519_add_payments_updated_by.sql`)로 bookings/consultations와 동일한 활동 추적 일관성 확보 — `logActivity('payment_update'/'payment_delete')` + `archiveBeforeDelete`. `app/(dashboard)/patients/page.tsx` PaymentSection에 인라인 편집(✏️/🗑️ 행 액션 → 시술명·금액·결제일 input 즉시 편집·저장·취소). agency_staff는 결제 생성 API와 동일 일관성 유지(`user.clinic_id` null → `checkClinicAccess` false)로 버튼 미노출. |
| 2026-05-18 | 광고 소재 페이지 병원 필터 연동(`app/(dashboard)/admin/ad-creatives/page.tsx`): 사이드바 ClinicContext의 `selectedClinicId`를 `useClinic()`으로 구독하고 `/api/admin/ad-creatives?clinic_id=…` 쿼리에 전달. 사이드바에서 병원 변경 시 자동 재조회(useEffect deps). 신규 등록 다이얼로그 오픈 시 선택된 병원을 폼에 기본 채움. API 라우트는 이미 `clinic_id` 파라미터를 지원하고 있어 서버 변경 불필요. |
| 2026-05-13 | CI: E2E 워크플로우의 test step에 `SUPABASE_SERVICE_ROLE_KEY` 가드 추가. Repository Secrets 미설정 시 dev server가 `supabaseKey is required` 로 모든 페이지를 빈 body로 응답해 11개 테스트가 timeout 되던 문제 회피. secrets 누락 시 test step skip + warning 출력 → 워크플로우 success. 향후 secrets 설정 시 자동으로 테스트 실행 재개. |
| 2026-05-13 | 보안: 모든 `public` 스키마 테이블 38개에 RLS 활성화(`supabase/migrations/20260513_enable_rls_all_public_tables.sql`) — Supabase Linter `rls_disabled_in_public` 경고 일괄 해소. Samantha는 `serverSupabase()` service_role로만 DB 접근하므로 자체 동작 영향 0이지만, 노출된 anon 키(`NEXT_PUBLIC_SUPABASE_ANON_KEY`)로 외부에서 PostgREST에 직접 접근해 모든 데이터를 읽어가는 공격 표면 차단. 정책(POLICY)은 의도적으로 추가하지 않음(service_role 자동 우회로 충분). `lib/CLAUDE.md` 에 신규 테이블 마이그레이션 시 RLS 활성화 필수 정책 명시. |
| 2026-05-13 | CI: `.github/workflows/e2e.yml` 워크플로우 즉시 실패(0초) 버그 fix. step의 `if:` 조건에서 `secrets` 컨텍스트 직접 참조(`if: ${{ secrets.E2E_SUPERADMIN_EMAIL != '' }}`)는 GitHub 유효성 검증이 거부함 → 모든 환경변수를 job-level `env:` 로 한 번 정의 후 `if: env.E2E_SUPERADMIN_EMAIL != ''` 패턴으로 전환. step별 env 중복 제거. |
| 2026-05-13 | 데모 모드(demo_viewer)에 superadmin 관리 페이지 4개 추가: `/admin/landing-pages`, `/admin/ad-creatives`, `/admin/clinics`, `/admin/users`. `lib/demo/guard.ts` 페이지/API 화이트리스트 확장(`/api/admin/landing-pages`, `/api/admin/ad-creatives`, `/api/admin/users` 추가, `/api/admin/clinics`는 기존 포함, `[id]/api-configs`는 prefix 매칭 자동). `lib/demo/fixtures/admin.ts` 신설(LP 8개·Creative 12개·User 11개·api_configs 동적). 각 admin page의 `!== 'superadmin'` 가드를 `!== 'superadmin' && user?.role !== 'demo_viewer'` 로 완화. `components/Sidebar.tsx`의 슈퍼어드민 섹션을 `(isSuperAdmin \|\| isDemoViewer)` 로 열되 UTM/리드수집/로그인로그/시스템설정은 `isSuperAdmin` 전용 유지. Write 액션은 `hooks/useDemoGuard.ts` 의 `blockDemoWrite()` 로 23개 핸들러(handleSave/Edit/Delete/Duplicate/FileUpload/toggle\*/openErpLinkDialog/handleErpLink/openNotifyDialog/handleNotifySave/openPermDialog/savePermissions/openPwDialog/submitPasswordReset) 사전 차단해 토스트만 노출(백엔드 405 안전망 그대로). 5개 admin GET API에 `if (user.role === 'demo_viewer') return apiSuccess(fixture)` 분기 추가. |
| 2026-05-13 | 광고 동기화 토스트 표시 fix: `app/(dashboard)/ads/page.tsx` 의 `handleSync` 가 `/api/ads/sync` 응답을 객체로 잘못 파싱(`data.results?.meta`)해 항상 "Meta: 0" 으로 표시되던 버그. 실제 응답은 `results: [{platform, count, error}]` 배열. 배열을 플랫폼별로 합산하고 `platformLabel` 로 라벨화(meta_ads→Meta 등). Dable/Naver/Kakao 등 추가 플랫폼 자동 표시 + 에러 발생 시 `toast.warning` 으로 분리 표기. |
| 2026-05-13 | Meta Graph API 버전 관리 정비: `META_API_VERSION` / `META_GRAPH_BASE` 상수 분리(`lib/services/metaAds.ts` 3곳 하드코딩 제거), v19.0 → **v25.0** 업그레이드(v19.0 sunset 임박 대응). `isFieldRemovedError` 헬퍼로 응답 error code 100 + "nonexisting field" 패턴 감지해 campaign insights / ad insights / ad creative 3개 호출 지점에 deprecation 명시 로깅 추가 → 향후 또 필드 제거 시 즉시 인지 가능. 운영: 분기별 Meta Marketing API Changelog 자동 점검 routine 등록(매 1/4/7/10월 1일 09:00 KST 실행, claude.ai/code/routines). |
| 2026-05-13 | 광고 성과 소재별 spend 정합성 fix(2건): (1) `lib/services/metaAds.ts` — Meta Graph API v19.0에서 제거된 `creative.effective_link` 필드 호출이 항상 throw하던 문제. fields 를 `object_story_spec{link_data{link},video_data{call_to_action}}` + `asset_feed_spec{link_urls}` 로 교체하고 `extractUtmContentFromCreative` 폴백 체인(url_tags → link_data → video CTA → asset_feed)으로 전환 → `ad_stats.utm_content`가 비정상 NULL로 들어오던 버그 해소. (2) `app/api/ads/creatives-performance/route.ts` — `creativeKeyLookup` 을 길이 내림차순으로 정렬해 긴 검색키(`울쎄라피_79_c`)가 짧은 키(`울쎄라피_79`)보다 먼저 매칭되도록 변경. `adIdMatchedByAdName` set 추가해 ad_name 부분문자열로 등록 소재에 귀속된 ad_id 가 별도 미등록 ad_id 표시 분기에도 출력돼 spend가 정확히 2배로 잡히던 중복 카운트 제거. |
| 2026-05-13 | 캠페인 리드 상태값 `consulting`(상담중) 추가 — `LEAD_STATUS_CONFIG`(campaigns/page.tsx) + `VALID_LEAD_STATUSES`(/api/leads/[id]). 상태 흐름 신규→부재→상담중→예약완료 사이 단계. 색상 cyan (기존 `consulted` legacy=amber와 구분). 6개 상태 그리드(`sm:grid-cols-6`)에 정확히 맞음 |
| 2026-05-12 | perf/UI: 예약/결제 메모 영역 통합 + 속도 개선 — 별도 "리드 메모" 아코디언 제거. 예약 정보 메모 영역 안에 리드 단계 타임라인(읽기) + 예약 메모(편집) 세로 배치. `/api/bookings` 응답에 booking별 `lead_notes` 배열을 prefetch (단일 bulk 쿼리) → 펼침/표시 시 추가 fetch 0건. `/api/patients/[id]/lead-notes` 엔드포인트 제거. `LeadNotesTimeline`을 props 기반 presentational 컴포넌트로 리팩토링 |
| 2026-05-12 | perf/UI: 캠페인 리드 메모 펴치기 지연 제거 — `/api/campaigns?campaign=…` 응답에 `notes: LeadNote[]` 전체 배열을 prefetch로 포함 (펼침 시 추가 fetch 불필요). 날짜 포맷 M/D → `formatDateTime` "M월 D일 HH:MM" (campaigns LeadCard + LeadNotesTimeline 양쪽 통일) |
| 2026-05-12 | 캠페인 리드 메모 다건화: `lead_notes` 테이블 도입(타임라인), 단일 `leads.notes` 컬럼 제거 + 1차 메모로 이관, /api/leads/[id]/notes CRUD 추가, LeadCard 인라인 확장 + 1차/2차/3차 표기 |
| 2026-04-21 | 소재별 CPL 계산 개선(`app/api/ads/creatives-performance/route.ts`): 직접 매칭 4단계 체계 구축. (1) `ad_stats.utm_content` ↔ `lead.utm_content` 직접, (2) `ad_stats.ad_id` ↔ `lead.utm_content`(Meta `{{ad.id}}` 매크로 대응), (3) `ad_stats.ad_name` 부분문자열에 `ad_creatives.utm_content`/`name` 포함 시 ad 레벨 귀속(Meta ads_read 권한 부재로 url_tags 추출 실패한 경우 대응), (4) 어디에도 안 잡힌 행만 campaign 풀 fallback. 이중 집계 방지 및 `campTotalLeadsMap` 분모에서 direct 매칭 소재 리드 제외. |
| 2026-04-21 | Dable Ads 연동: `lib/services/dableAds.ts` 신규 (daily_report, group_by_campaign=1, 7일 롤링 재조회). adSyncManager 확장, 연결 테스트(Budget Report 기반), `SYNC_ENABLED_PLATFORMS`에 `dable_ads` 추가. `lib/platform.ts` 필드 `advertiser_id` → `client_name` 변경 (Dable은 path에 URL-encoded client_name 사용). Budget Report는 별도 UI 없이 연결 테스트 용도로만 사용(타 플랫폼 balance 미지원이라 일관성 유지). |
| 2026-03-19 | CLAUDE.md 재설계: 모듈 분리, 검증 루프, 도메인 용어, 네이밍 컨벤션, 팀 가이드 추가 |
| 2026-03-19 | 원격 변경 병합: ClinicContext, archive, error-alert, channel, date 유틸, deleted_records, E2E 상세 |
| 2026-03-19 | 검증 규칙에 '전체 맥락 코드 리뷰' 단계 추가 — 구현부만 단독 검토하지 않고 호출자/데이터 흐름/역할별/기존 패턴과의 정합성까지 확인 |
| 2026-03-19 | 디렉토리 구조에 공개 페이지(privacy, terms), 앱 아이콘(icon.tsx, apple-icon.tsx) 추가 |
| 2026-03-20 | 디렉토리 구조에 `components/ads/` 추가 — 광고 성과 페이지 3탭 구조 재구성 (4개 신규 API, 9개 신규 컴포넌트) |
| 2026-03-20 | 언론보도 다중 키워드 지원: `press_keywords` 테이블, 키워드 CRUD API, pressSync 다중 키워드 검색, clinic_staff 언론보도 접근 허용 |
| 2026-03-20 | 순위 모니터링 키워드 삭제 기능 추가 (DELETE API + AlertDialog UI) |
| 2026-03-20 | 언론보도 DateRangePicker 추가, Google News 검색 기간 제한(6개월) |
| 2026-03-20 | 광고 성과 날짜 포맷 불일치 수정: ISO→YYYY-MM-DD 통일, timestamptz KST 명시 (8개 API 파일) |
| 2026-03-23 | 예약/결제 관리 필터·정렬 추가: DateRangePicker, 상태/유입경로/결제 필터, SortSelect 공용 컴포넌트(`components/common/sort-select.tsx`) |
| 2026-03-23 | 캠페인 리드 필터·페이지네이션: 목록/상세 검색·채널·정렬, DateRangePicker, 50건 페이지네이션, `normalizeChannel` 재사용(`lib/channel.ts`) |
| 2026-03-23 | UI/UX 감사 P0~P3: `prefers-reduced-motion`, StatsCard/퍼널/상태배지 키보드 접근성, 하드코딩 hex→COLORS 상수, `text-[10px]`→`text-xs` 상향, `navLinkClass()` 추출, Firefox 스크롤바, transition `duration-200` 통일, `as any` 제거 |
| 2026-03-23 | 페이지별 브라우저 탭 제목: thin server layout.tsx 래퍼 패턴 (18개 페이지), `metadata.title.template` 설정 |
| 2026-03-23 | Samantha 브랜드: 서비스명 확정, 컬러 마이그레이션 Indigo→Blue (15개 파일), `docs/BRAND.md` 생성 |
| 2026-03-23 | agency_staff 메뉴 권한에 키워드 관리 항목 추가 |
| 2026-03-24 | glitzy-web ERP 연동 가이드 문서 추가 (`docs/INTEGRATION.md`): 견적서/계산서 읽기 전용 연동 설계 |
| 2026-03-24 | MediChecker 통합: 의료광고법 제56조 AI 검증 기능 (7단계 파이프라인, 5 DB 테이블, 3 API, 8 컴포넌트, 사이드바 메뉴) |
| 2026-03-24 | MediChecker UI/UX 감사: 2컬럼 레이아웃, 심각도 그룹핑, 컴팩트 카드, A11y 13건 수정, getRiskLevel 유틸 추출 |
| 2026-03-24 | 검증 규칙에 '문서 업데이트 필수' 단계 추가 — 코드 리뷰 완료 후 관련 문서(CLAUDE.md, API.md, COMPONENTS.md 등) 반드시 업데이트 |
| 2026-03-24 | MediChecker Phase 2: logActivity 연동 + 이력 상세 보기 |
| 2026-03-24 | MediChecker Phase 3: agency_staff 메뉴 권한에 `medichecker` 항목 추가 (MENU_OPTIONS) |
| 2026-03-24 | ERP 연동: glitzy-web 견적서/계산서 읽기 전용 프록시 (erpClient, API 2개, UI 2컴포넌트, Sheet 상세) |
| 2026-03-24 | ERP Phase 2: 견적서 승인/반려 (PATCH API, erpClient method/body 확장, Sheet 내 승인/반려 버튼, 반려 사유 다이얼로그) |
| 2026-03-24 | fix: 대시보드 퍼널 API `applyDateFilter` 날짜 이중 타임존 버그 수정 (전 단계 0명 표시) |
| 2026-03-26 | 광고 backfill API (`/api/admin/backfill-ads`): 특정 병원의 과거 광고 데이터 일괄 수집 (최대 90일, CRON_SECRET 인증) |
| 2026-03-26 | `sanitizeUrl()` 도입: URL용 sanitize 함수 추가 (`&` 보존, 위험 스킴 차단). `sanitizeString`이 URL의 `&`를 제거하여 CAPI event_source_url/DB inflow_url 깨지는 버그 수정 (6곳 교체) |
| 2026-03-26 | Ad 레벨 수집: `ad_stats` 테이블, `fetchMetaAdStats()` (페이지네이션+url_tags/effective_link→utm_content), 소재별 성과에 광고 지표(지출/노출/클릭/CPC/CTR/CPL) 통합, 캠페인 CPL ad_stats 경유 매칭 |
| 2026-03-26 | StatsCard 동적 폰트 크기: 값 길이에 따라 폰트 자동 축소 (`getValueSizeClass`), `truncate` 제거 → `break-all` 적용. 좁은 카드에서 금액 잘림 방지 |
| 2026-03-26 | DateRangePicker 개선: 시작일/종료일 명시 표시, 팝오버 상단 선택 상태, "이번 달" 프리셋, 캘린더 2개월 표시. 광고 성과 기본값 이번 달로 변경 |
| 2026-03-26 | fix: 캠페인 CPL + 소재별 광고 지표 — inflow_url utm_id 기반 매칭 (ads_read 권한 불필요), 날짜 표시 "최근 N일" → "M.D ~ M.D" 통일 |
| 2026-03-26 | 전체 프로젝트 감수 및 수정: (1) GAQL 쿼리 파라미터 인젝션 방어 (googleAds.ts), (2) `session?.user as any` 제거 14개 페이지, (3) 대시보드 API 인라인 `applyFilter` → 중앙 `applyClinicFilter`/`applyDateRange` 교체 6개 파일, (4) Recharts 툴팁 `any` → `ChartTooltipProps` 타입 적용 9개 컴포넌트 (`types/recharts.d.ts` 신규), (5) API try-catch 래핑 6개 파일, (6) `NextResponse.json` → `apiSuccess` 7개 API, (7) 차트 컬러 중앙화 `lib/chart-colors.ts` 신규 + 9개 컴포넌트 교체, (8) 다크모드 `--overlay` CSS 변수 도입 + 5개 UI 컴포넌트 적용, (9) 정렬 테이블 aria-sort/키보드 접근성 2개 컴포넌트, (10) 터치 타겟 44px 확대 3개 컴포넌트, (11) console.log → createLogger 1건, toISOString → getKstDateString 2건 |
| 2026-03-27 | 순위 모니터링 "함께많이찾는" 카테고리 추가: DB CHECK 제약조건 확장(`related`), API validCategories, UI 3개 페이지 CATEGORY_LABELS/LIST 업데이트 |
| 2026-03-30 | fix: 전체 프로젝트 KST 타임존 일관성 감사 및 수정 — (1) KPI API `split('T')[0]` UTC→KST 오변환 (`getKstDateString` 교체), (2) 콘텐츠 분석 `toISOString().slice(0,7)` UTC 월 오류 (`getKstDateString(toUtcDate())` 교체), (3) 순위입력 날짜 이동 타임존 미지정 (`+09:00` 추가), (4) stat_date `split('T')[0]` → `slice(0,10)` 정리 (2곳), (5) E2E 헬퍼 `toISOString()` → KST 변환. `toISOString().split('T')[0]` 패턴 프로젝트에서 완전 제거 |
| 2026-03-30 | TikTok Ads API 연동: (1) `data_level` 필수 파라미터 추가, (2) `fetchTikTokAdStats` ad 레벨 수집 신규 (ad_stats 저장), (3) 캠페인+ad 레벨 페이지네이션 공통 헬퍼 `fetchTikTokReport`, (4) adSyncManager Meta 동일 병렬 수집 구조, (5) 소재별 성과 API에 utm_content 없는 ad_stats(TikTok) ad_id 기준 표시, (6) 90일 backfill 완료 (clinic_id=20) |
| 2026-03-30 | 대시보드 재설계: (1) 기본 날짜 "오늘"→"이번 달", (2) KPI 6카드→5카드(광고비/리드+오늘/CPL/매출/ROAS), (3) 퍼널 5단계→3단계(리드→예약→결제)+인사이트, (4) TodaySummary/ChannelChart/CplRoasChart 제거, (5) RecentLeads 최근 8건 피드 신규, (6) ChannelTable 정렬 가능 테이블 신규(clicks/impressions/ctr 추가), (7) 채널 API에 clicks/impressions/ctr 필드 추가 |
| 2026-03-30 | 시스템 메뉴 토글: `system_settings` 테이블, 슈퍼어드민 설정 페이지(`/admin/settings`), 사이드바 동적 숨김 메뉴 로드. 하드코딩 `hidden` 플래그 → DB 기반 동적 제어로 전환 |
| 2026-03-30 | 광고 성과 3탭 지표 재배치: (1) 성과 개요: KPI 8→5카드(ROAS/전환율/CAC→매출귀속), 매체비교에 노출/클릭 추가+ROAS/전환율 제거, 퍼널 5→3단계(2-Zone+미니카드), LP분석→캠페인탭 이동 (2) 캠페인분석: LP분석 추가(mode prop delivery/full), 소재별 13→10컬럼(결제/전환율/매출 제거) (3) 매출귀속: KPI 3→6카드(ROAS/전환율/CAC 추가), 전환퍼널·채널별 매출비중·ROAS 추이·LP 전환 테이블 신규 |
| 2026-03-30 | 외부 API: `GET /api/external/ad-spend` — 병원별 월간 광고 실집행비(매체별) + SMS 발송 건수. `withExternalAuth` 미들웨어 신규 (`EXTERNAL_SERVICE_KEY` Bearer 인증). glitzy-web 결산용 |
| 2026-03-30 | 광고 성과 UI 개선: 퍼널 수직 스텝 카드, 소재별 성과 10건 페이지네이션, 캠페인 행 클릭→소재 필터링 |
| 2026-03-30 | 고객 여정 타임라인에서 챗봇 발송 단계 제거 |
| 2026-03-30 | fix: 고객 상세 모바일 Sheet 헤더 중복 제거 — SheetHeader `sr-only` + CustomerDetail `hideHeader` prop 추가 |
| 2026-03-30 | CLAUDE.md 재설계: 200줄 이내 압축, 변경 이력 `docs/CHANGELOG.md` 분리, 디렉토리 구조 압축, 팀 가이드 추가 |
| 2026-04-01 | 예약/결제 관리 DateRangePicker 미래 날짜 선택 허용 (`allowFuture` prop) |
| 2026-04-01 | 예약/결제 관리 DateRangePicker 예약 날짜 도트 표시 (`bookedDates` prop, `modifiers` 활용) |
| 2026-04-01 | 예약/결제 관리 캘린더 드래그앤드롭: `@dnd-kit/core` 도입, 월간/주간/일간 뷰에서 예약 카드 드래그→날짜/시간 변경 (확인 다이얼로그, cancelled/noshow 드래그 비활성화) |
| 2026-04-01 | 캘린더 UX 개선: 일간 뷰 10분 단위 슬롯(10:00~19:50), 현재 시간 빨간 구분선+자동 스크롤, 월간/주간 뷰 전체 예약 표시(slice 제한 제거), 취소/노쇼 취소선+투명도 시각 구분, DragOverlay/확인 다이얼로그 디자인 개선 |
| 2026-04-07 | fix: 시술별 매출 비중 KPI 매출 불일치 수정 — leads 간접 조회(200건 제한)에서 payments 직접 조회로 변경 (`/api/dashboard/treatment-revenue` 신규) |
| 2026-04-07 | feat: 광고 플랫폼 2계층 구조 도입 — `lib/platform.ts` 중앙 상수, campaign_type 컬럼 추가, platform 값 `meta_ads` 형식 통일, Naver/Kakao/Dable 신규 플랫폼 API 설정 UI |
| 2026-04-13 | feat: 랜딩페이지 제출 후 리다이렉트 URL 설정 — `landing_pages.redirect_url` 컬럼 추가, 관리 페이지 입력란, `/api/lp/render`에서 `__LP_DATA__.redirectUrl` 주입 후 폼 제출 성공 시 `location.href` 이동 (HTML 파일 무수정) |
| 2026-04-13 | feat: Demo 모드 (`demo_viewer` role) — 영업/영상 촬영용 실데이터 무결성 보장 샌드박스. 4겹 방어(role / 화이트리스트 미들웨어 / API 래퍼 / 핸들러 분기), `/demo/enter?key=<DEMO_ACCESS_KEY>` 진입 + `/demo/exit` 퇴장, `lib/demo/fixtures/` (병원 6개 × 12개월 deterministic 광고 데이터 + 에피소드), 18개 GET API fixture 분기, 모든 Write 405 차단, Cron/동기화 접근 불가, `DemoBadge` UI, `scripts/seed-demo-user.ts` |
| 2026-04-17 | feat: 거래처 양방향 동기화 — `clinics.erp_client_id TEXT` 컬럼, webhook 수신, ERP 문서 `erp_client_id` 전환, 거래처 검색 프록시, 클리닉 UI 거래처 연결 다이얼로그 |
| 2026-04-17 | fix: 거래처 ID 타입 INTEGER→TEXT(UUID), API 경로 `/external/clients`→`/clients` (이중 경로 수정), erpClient 함수 시그니처 `number`→`string` |
| 2026-04-17 | refactor: 병원 등록 거래처 UI Agatha 기준 재구성 — 기본 "거래처 선택" 모드, 이미 연결된 거래처 숨김, 전체 페이지네이션 로드, "새로 생성" 사업자/담당자 필드, branch_name 지점명 표시 |
| 2026-04-17 | feat: Glitzy 아이콘 패밀리 — favicon 다크 "S" 레터, apple-icon, OG 이미지 Brutalist dark, layout.tsx OG/Twitter metadata |
| 2026-04-17 | fix: DialogContent `aria-describedby` 경고 해결 (3개 다이얼로그 DialogDescription 추가) |
| 2026-04-17 | fix: 로그인 username 양끝 공백 trim — 비밀번호 매니저 자동완성 시 trailing space 포함으로 `user_not_found` 실패하던 문제 (`lib/auth.ts:70`) |
| 2026-04-20 | feat: 슈퍼어드민 비밀번호 재설정 — `PATCH /api/admin/users/[id]/password` 신규 (bcrypt cost 12, `password_version` 증가로 기존 세션 무효화, `activity_logs` 기록), 계정 관리 페이지 KeyRound 아이콘 버튼 + 재설정 다이얼로그 |
| 2026-04-21 | fix: 랜딩페이지 전환 중복 집계 방지 (`app/api/lp/render/route.ts`) — (1) LeadQueue.retry()에 `X-Lead-Retry: 1` 헤더 추가, (2) fetch wrapper가 retry 요청이면 `form_submit` dataLayer.push / 오버레이 / 리다이렉트 스킵 → 페이지 로드 시 localStorage 재전송이 GTM Dable/Meta 전환으로 이중 집계되던 버그 해소, (3) bodyObj에 `idempotency_key` 존재 시 wrapper의 중복 `LeadQueue.save()` 생략 (HTML 폼 핸들러와 이중 저장 제거) |
| 2026-05-04 | chore: 주간 리포트 SMS cron 중지 — `vercel.json`에서 `/api/cron/weekly-report` 스케줄 제거. 라우트(`app/api/cron/weekly-report/route.ts`)와 `lib/services/weeklyReport`는 보존하여 재개 시 cron 항목만 복구하면 됨 |
